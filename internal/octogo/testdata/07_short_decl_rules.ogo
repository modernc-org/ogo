// ERROR unexpected identifier "topLevel"
topLevel := 100 

func testShortDecl() {
    x := 1
    
    //TODO ERROR "no new variables on left side of :="
    x := 2 
    
    // Valid: at least one new variable (y)
    x, y := 3, 4 
    
    // (Assuming "hello" evaluates to a string type)
    //TODO ERROR "cannot redeclare x as different type|type mismatch"
    x, z := "hello", 5 
    
    {
        // Valid: This is shadowing, NOT redeclaration, because it's in a new block
        x := 10
    }
}
